services:
  payment:
    build:
      context: ./../../../
      dockerfile: deploy/docker/payment/Dockerfile
    container_name: payment-services
    env_file:
      - ./.env # путь к env относительно compose-файла
    restart: unless-stopped
    networks:
      - microservices-net

networks:
  microservices-net:
    name: microservices-net
    external: true
